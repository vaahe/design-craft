// #include "cuttingoptimizer.h"

// QList<QString> CuttingOptimizer::optimize(const FurnitureModel &model, float maxArea) {
//     QList<QString> result;
//     const auto parts = model.getParts();

//     // Example dummy logic: just logging the model's pieces
//     for (const FurniturePart& piece : parts) {
//         QString desc = QString("Cut %1 x %2").arg(piece.width).arg(piece.height);
//         result.append(desc);
//     }

//     qDebug() << "Max area allowed:" << maxArea;
//     return result;
// }
